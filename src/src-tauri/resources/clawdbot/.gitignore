# Ignore installed node_modules — only intentionally committed shims (e.g. node_modules/chalk/) are tracked.
node_modules/
!node_modules/chalk/

# Provisioner writes bot tokens here; never commit this file.
.telegram-tokens.env
