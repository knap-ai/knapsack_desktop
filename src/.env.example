# Knapsack Environment Variables
# Copy this file to .env and configure as needed

# =============================================================================
# REQUIRED VARIABLES (for dev builds)
# =============================================================================

# Backend API server URL
# For dev builds using knap.ai authentication (default), use:
VITE_KN_API_SERVER=https://knap.ai

# Google OAuth Client ID (used for Google sign-in)
VITE_GOOGLE_CLIENT_ID=963137762742-tqs7tiv9bkh80t5io8hm5q8e798ab85s.apps.googleusercontent.com

# Microsoft OAuth Client ID (required for build, can be placeholder if not using Microsoft)
MICROSOFT_CLIENT_ID=your-microsoft-client-id

# =============================================================================
# OPTIONAL: SELF-HOSTED AUTHENTICATION
# =============================================================================
# If you want to run Knapsack completely self-hosted without depending on
# knap.ai for OAuth token exchange, set your own Google OAuth client secret:
#
# GOOGLE_CLIENT_SECRET=your-google-client-secret
#
# When GOOGLE_CLIENT_SECRET is set at runtime, Knapsack will exchange OAuth
# codes directly with Google's token endpoint instead of routing through
# knap.ai. This allows fully offline/self-hosted operation.
#
# To set up self-hosted authentication:
# 1. Create a Google Cloud project at https://console.cloud.google.com
# 2. Enable the following APIs: Gmail, Google Drive, Google Calendar
# 3. Configure OAuth consent screen
# 4. Create OAuth 2.0 credentials (Desktop application)
# 5. Add http://localhost:8897/api/knapsack/google/signin as authorized redirect URI
# 6. Set VITE_GOOGLE_CLIENT_ID to your client ID
# 7. Set GOOGLE_CLIENT_SECRET to your client secret

# =============================================================================
# OPTIONAL: ERROR TRACKING
# =============================================================================

# Sentry DSN for error tracking (optional)
# VITE_SENTRY_DSN=your-sentry-dsn

# Sentry authentication token for source map uploads (optional)
# SENTRY_AUTH_TOKEN=your-sentry-auth-token
